doctype html
html
    include includes/tags
    body 
        h1(id="title")
        h2(id="pagename")
        div(id="axisholder")
        h2(id="score1")
        h3(id="score2")
        p(id="desc")
        h2(id="simple_results")
        canvas(id="results1")
        button(id="download1")
            include includes/download.svg
        h2(id="advanced_results")
        canvas(id="results2")
        button(id="download2")
            include includes/download.svg
        button(id="full_list" class="small")
        button(id="back_button")
        script(type="application/javascript") window.VERSION = "#{version}"
        script(type="module" src="./dist/results.js")