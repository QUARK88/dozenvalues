doctype html
html
    include includes/tags
    body 
        h1(id="title")
        h2(id="pagename")
        p(id="instructiontext")
        button(id="quiz_button")
        button(id="quiz_random_button")
        button(id="back_button")
        if inline_js
            script(type="module") !{js["instructions"]}
        else
            script(type="module" src="./dist/instructions.js")