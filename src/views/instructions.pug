doctype html
html
    include includes/tags
    body
        //-span(id="switchlabel")
        //-label(class="switch" id="themeswitch")
        //-    input(type="checkbox" id="box")
        //-    span(class="slider")
        a(
            href="./index.html"
            class="index-corner"
            aria-label="Go back to main page"
            )
            include includes/index.svg
        h1(id="title")
        h2(id="pagename")
        p(id="instructiontext")
        button(class="startbutton" id="quiz_button")
            span(id="quiz_text")
        button(class="startbutton" id="quiz_random_button")
            span(id="quiz_random_text")
        button(class="backbutton" id="back_button")
            span(id="back_text")
        if inline_js
            script(type="module") !{js["instructions"]}
        else
            script(type="module" src="./dist/instructions.js")